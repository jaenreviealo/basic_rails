<%= form_for @resident, url: resident_path(@resident), method: 'put', html: { autocomplete: :off, class: 'form col-md-5', id: 'resident-form', style: 'margin: auto;' } do |f| %>
  <%= render partial: 'residents/form', locals: {f: f} %>
<% end %>